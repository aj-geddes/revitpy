[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "revitpy"
dynamic = ["version"]
description = "Modern Python Framework for Revit Development"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [
    { name = "RevitPy Team" },
]
keywords = [
    "revit",
    "bim",
    "architecture",
    "construction",
    "automation",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "pydantic>=2.5,<3",
    "typing-extensions>=4.0.0",
    "aiofiles>=23.0.0",
    "loguru>=0.7.0",
    "httpx>=0.25.0",
    # <12 is required by specklepy's gql dependency; revitpy.ai supports both
    # the legacy (<13) and the asyncio (>=13) websockets server APIs.
    "websockets>=11.0.0",
    "pyyaml>=6.0.0",
    "click>=8.0.0",
    "rich>=13.0.0",
    "jinja2>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
    "mypy>=1.10.0",
    "ruff>=0.15.4",
    "pre-commit>=3.0.0",
    "psutil>=5.9.0",
]
ifc = ["ifcopenshell>=0.8.0", "defusedxml>=0.7.1", "ifctester>=0.8.0"]
interop = ["specklepy>=3.0.0"]
sustainability = []  # uses existing httpx
cloud = []  # uses existing httpx, aiofiles
ai = []  # uses existing websockets, jinja2
all = [
    "ifcopenshell>=0.8.0",
    "defusedxml>=0.7.1",
    "ifctester>=0.8.0",
    "specklepy>=3.0.0",
]

[project.scripts]
revitpy = "revitpy.cli:main"

[project.urls]
Homepage = "https://github.com/aj-geddes/revitpy"
Documentation = "https://aj-geddes.github.io/revitpy/"
Repository = "https://github.com/aj-geddes/revitpy"
Issues = "https://github.com/aj-geddes/revitpy/issues"
Changelog = "https://github.com/aj-geddes/revitpy/blob/main/CHANGELOG.md"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.targets.wheel]
packages = ["revitpy"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_no_return = true
strict_equality = true
show_error_codes = true
# Relax rules with many pre-existing violations; tighten incrementally
disable_error_code = [
    "attr-defined",
    "assignment",
    "var-annotated",
    "valid-type",
    "operator",
    "arg-type",
    "misc",
    "union-attr",
    "override",
    "return-value",
    "index",
    "call-overload",
    "no-redef",
    "exit-return",
    "has-type",
    "dict-item",
    "truthy-function",
    "type-var",
    "type-abstract",
    "import-untyped",
]

# numpy (pulled in by ifcopenshell) ships stubs with 3.12-only syntax; don't
# parse them when checking against the 3.11 baseline.
[[tool.mypy.overrides]]
module = ["numpy", "numpy.*", "shapely", "shapely.*"]
follow_imports = "skip"
follow_imports_for_stubs = true

[tool.ruff]
target-version = "py311"
line-length = 88
extend-exclude = ["cli/templates/"]

[tool.ruff.format]
# ruff >= 0.16 also formats code blocks in Markdown; docs are formatted by hand.
exclude = ["*.md"]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
    "S",  # flake8-bandit security rules
]
ignore = [
    "E501",  # line too long, handled by ruff format
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
    "B904",  # raise from within except - TODO: fix these
    "E722",  # bare except - TODO: fix these
    "E741",  # ambiguous variable name
    "UP007", # Use X | Y for type annotations
    "B007",  # unused loop variable
    "B023",  # function not binding loop variable
    "B017",  # pytest.raises(Exception)
    "F811",  # redefinition of unused
    "S110",  # try-except-pass
    "S112",  # try-except-continue
    "S603",  # subprocess call
    "S607",  # partial executable path
    "N802",  # function name should be lowercase
    "N805",  # first argument should be self
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/**/*" = ["S101", "D", "F401", "E402"]
"**/tests/**/*" = ["S101", "S105", "S106", "D", "F401", "E402"]
"revitpy/performance/**/*" = ["F401"]
"cli/**/*" = ["F401", "E402", "B904"]
"revitpy-package-manager/**/*" = ["F401", "E402", "F403", "F405", "S"]
"proof-of-concepts/**/*" = ["S311"]

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q --strict-markers --strict-config -m 'not slow'"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "slow: long-running tests, excluded by default (run with -m slow)",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "performance: performance and benchmark tests",
    "benchmark: benchmark tests",
    "security: security-focused tests",
    "regression: regression tests for fixed bugs",
    "compatibility: cross-version compatibility tests",
    "mock_revit: tests using the mock Revit environment",
    "real_revit: tests that require a real Revit installation",
    "e2e: end-to-end workflow tests",
]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["revitpy"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
