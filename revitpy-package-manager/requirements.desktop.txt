# Minimal requirements for desktop package registry
# Focused on essential dependencies only (exact pins for reproducible images).
# Test tooling lives in the `test` extra: pip install -e ".[test]"

# Core web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# Database (SQLite support)
sqlalchemy[asyncio]==2.0.54
aiosqlite==0.22.1

# File handling
aiofiles==25.1.0

# HTTP client for registry communication
httpx==0.28.1
aiohttp==3.14.3

# Security
cryptography==50.0.1
bcrypt==5.0.0
PyJWT==2.15.0

# Package management
packaging==26.3

# CLI and output
click==8.5.0
rich==15.0.0
typer==0.27.2

# Configuration
pyyaml==6.0.3
python-multipart==0.0.32

# Optional caching (Redis)
redis==8.1.0

# Utilities
python-dateutil==2.9.0.post0
python-slugify==9.1.1
